# Outillage de développement — tests, lint, typage.
# Usage : pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27          # TestClient FastAPI
ruff>=0.6
mypy>=1.11
pip-audit>=2.7       # audit de vulnérabilités des dépendances (CI + local)
build>=1.0           # construction du wheel/sdist de release (python -m build)
