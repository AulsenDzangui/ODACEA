# Backend HTTP (expose le moteur au front React/Next)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# LLM (interface unifiée — Anthropic, OpenAI, Google, Ollama, LM Studio, JAN)
# Plancher à 1.84 : les versions antérieures portent des vulnérabilités connues
# (PYSEC-2026-388, PYSEC-2026-3479), corrigées en 1.84.0.
# Pas de borne haute. litellm a gagné une extension Rust en cours de série : les
# versions ~1.9x sans roue précompilée pour l'interpréteur courant retombent sur
# l'archive source et échouent faute de Rust — et comme pip résout tout le graphe
# avant d'installer, AUCUNE dépendance n'est alors installée. La 1.94 publie des
# roues jusqu'à cp314 (Windows compris) ; pip prenant la plus récente, laisser la
# borne ouverte est ce qui installe le mieux. Sur Python 3.14, exiger >= 1.94.
litellm>=1.84

# Données
# Borne haute : pandas 3 change des comportements par défaut (types de chaînes,
# copy-on-write). À lever après validation explicite de la suite sur pandas 3.
pandas>=2.2.0,<3

# Enrichissement mécanique des descriptions (étape `enrich`, lecture locale des
# binaires bureautiques — aucun appel réseau, pas d'OCR)
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23

# Utilitaires
python-dotenv>=1.0.0
